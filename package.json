{
  "name": "lotm-tcg",
  "version": "0.1.0",
  "private": true,
  "description": "Beyond the Veil - TCG Lord of the Mysteries",
  "scripts": {
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server dev",
    "dev": "pnpm run --parallel dev:client dev:server",
    "build": "pnpm --filter game-engine build && pnpm --filter client build && pnpm --filter server build",
    "lint": "pnpm -r lint"
  },
  "pnpm": {
    "overrides": {}
  }
}
